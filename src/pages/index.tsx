import { Box } from '@chakra-ui/react';
import { css } from '@emotion/react';

const Index = () => (
  <Box>
    <div
      css={css`
        text-align: center;
      `}
    >
      Page working!
    </div>
  </Box>
);

export default Index;
